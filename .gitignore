node_modules/
dist/
android/
ios/
# capacitor.config.json теперь настоящий файл проекта: в нём настройки
# фоновой проверки обновлений. Игнорируем только то, что генерит cap init.
capacitor.config.ts
capacitor.config.js
*.apk
.DS_Store
*.log
.vite/

# Сборки ПК-версии
desktop-out/

# Ключ подписи релиза. В git его класть нельзя: репозиторий публичный,
# и ключ в нём = любой может подписать свой APK как «обновление CHUBUGAMES».
# Лежит локально у автора и в CI приходит из Secrets (см. android-signing/README.md).
android-signing/*.p12
android-signing/*.jks
android-signing/keystore.properties
android-signing/PASTE-INTO-SECRETS.txt
**/chubgames.p12
.tmp-ui-render/
